# Handoff — c9h9 rebaseline → r564.3

Data: 2026-07-12 Europe/Warsaw

## Stan

- `c9h9.4` jest zamknięty i nie wymaga ponownej implementacji. Canonical
  WordPress draft create pozostaje wyłącznie przez typed ActionObject apply;
  `publication_allowed=false`, `destructive_allowed=false` i live UI pozostaje
  fail-closed bez bieżącego readiness/audytu.
- `c9h9.2` został zamknięty po aktualizacji
  `docs/audits/2026-07-10-cleanup-rebaseline.md` do `ba033433` oraz sprawdzeniu
  aktualnego grafu Beads.
- Live API: health `ok`; 98 919 metric facts; 4 574 refresh runs; 12
  connectorów, 9 skonfigurowanych i 2 bez credentials. GSC ma świeży odczyt,
  ale WordPress, GA4, Ahrefs i Ads pozostają stale.
- Dashboard usefulness audit: 14 surfaces, 12 `demo_ready`, 2 `review_ready`,
  `pass=true`. Focused content/action backend tests oraz dashboard
  ContentWorkflow/CommandCenter 17/17 przechodzą.

## Browser proof

Aktualne renderingi `/content-workflow` są w ignorowanym katalogu:

- `.local-lab/proof/continuation-2026-07-12/content-workflow-desktop.png`
- `.local-lab/proof/continuation-2026-07-12/content-workflow-mobile.png`
- odpowiadające pliki `.txt` z widocznym tekstem.

Mobile 390×844 pokazuje URL/temat, decyzję, blocker i bezpieczny CTA
„Otwórz decyzję i dowody” przed ciężkim kontekstem. CTA nie uruchamia zapisu.

## Aktywny następny Bead

`wilq-seo-r564.3` pozostaje `in_progress`. Implementacja mobile decision card
jest potwierdzona, ale świeży, nieblokowany candidate nadal zależy od
zewnętrznego refreshu. Nie zamykaj Beada na podstawie samego layoutu ani
`demo_ready` z usefulness audit.

## Następny bezpieczny krok

Sprawdź `GET /api/connectors` i content diagnostics po kolejnym read-only
refreshu. Jeśli candidate jest świeży i actionable, ponów browser proof desktop
i mobile oraz dopiero wtedy oceń zamknięcie `r564.3`. Jeśli nie, zapisz aktualny
external blocker i przejdź wyłącznie do innego gotowego Beada bez wymyślania
metryk ani nowych progów.
