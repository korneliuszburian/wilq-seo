"""Focused support leaves for API contract tests.

This package intentionally has no re-export barrel. Test modules import the
specific helper they need so factory ownership stays local and explicit.
"""
